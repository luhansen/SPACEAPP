/*
export const someGetter = (state) => {
}
*/

export const positions = (state, getters) => {
  return state.positions
}

export const currentText = (state, getters) => {
  return state.currentText
}
