export const displayDrawer = (state, getters) => {
  return state.displayDrawer
}

export const total = (state, getters) => {
  return state.total
}

export const mobileView = (state, getters) => {
  return state.mobileView
}
